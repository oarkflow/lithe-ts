{
	"name": "lithe-todo-example",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Lithe todo example with component composition, routing, SSR-safe output, and single-file production bundling.",
	"engines": {
		"node": ">=22.6.0"
	},
	"scripts": {
		"dev": "lithe dev",
		"build": "lithe build",
		"build:chunks": "lithe build --bundle=chunks",
		"check": "lithe check",
		"typecheck": "lithe typecheck",
		"clean": "rm -rf dist .lithe"
	},
	"dependencies": {
		"@oarkflow/lithe": "file:../.."
	},
	"devDependencies": { }
}
