import { getOwner, __setOwnerSequence } from './owner.ts';
function safe(value,seen=new WeakSet()){if(value==null||['string','number','boolean'].includes(typeof value))return value;if(typeof value==='bigint')return String(value);if(value?.__litheSignal)return safe(value.peek?.()??value.value,seen);if(Array.isArray(value))return value.map(v=>safe(v,seen));if(typeof value==='object'){if(seen.has(value))return'[Circular]';seen.add(value);const out={};for(const[k,v]of Object.entries(value))if(typeof v!=='function'&&typeof v!=='symbol')out[k]=safe(v,seen);seen.delete(value);return out;}return undefined;}
export function ownerTree(owner=getOwner()){if(!owner)return null;const contexts={};for(const[k,v]of owner.contexts)contexts[k.description||String(k)]=safe(v);return{id:owner.id,name:owner.name||null,disposed:owner.disposed,cleanups:owner.cleanups.length,contexts,resume:safe(owner.resume),children:[...owner.children].map(ownerTree)};}
export function serializeOwnerGraph(owner=getOwner()){return ownerTree(owner);}
export function createDetachedOwner(snapshot,parent=null){if(!snapshot)return null;const owner={id:snapshot.id,parent,cleanups:[],children:new Set(),contexts:new Map(),disposed:Boolean(snapshot.disposed),name:snapshot.name||null,resume:snapshot.resume||null,restored:true};for(const child of snapshot.children||[])owner.children.add(createDetachedOwner(child,owner));__setOwnerSequence(owner.id);return owner;}
