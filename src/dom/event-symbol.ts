function captureSnapshot(value,seen=new WeakSet()){if(value?.__litheSignal)return captureSnapshot(value.peek?.()??value.value,seen);if(value==null||typeof value==='string'||typeof value==='number'||typeof value==='boolean'||typeof value==='bigint')return typeof value==='bigint'?String(value):value;if(Array.isArray(value))return value.map(x=>captureSnapshot(x,seen));if(typeof value==='object'){if(seen.has(value))throw new TypeError('Resumable event captures must be acyclic.');seen.add(value);const out={};for(const[k,v]of Object.entries(value))if(typeof v!=='function'&&typeof v!=='symbol')out[k]=captureSnapshot(v,seen);seen.delete(value);return out;}if(typeof value==='undefined')return null;throw new TypeError('Resumable event captures must be serializable.');}
export function eventSymbol(module,exportName='default'){return Object.freeze({__litheEventSymbol:true,module,exportName,captures:null});}
export function capturedEventSymbol(module,exportName='default',captures=null){return Object.freeze({__litheEventSymbol:true,module,exportName,captures,snapshot:captureSnapshot(captures)});}
